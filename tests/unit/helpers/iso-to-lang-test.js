
import { isoToLang } from 'dashboard/helpers/iso-to-lang';
import { module, test } from 'qunit';

module('Unit | Helper | iso to lang');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isoToLang([42]);
  assert.ok(result);
});

